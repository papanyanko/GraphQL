steps:
  - id: build-backend
    name: "docker"
    args:
      - build
      - --file=Dockerfile
      - "--tag=$_ARTIFACT_REPOSITORY_IMAGE_NAME:$SHORT_SHA"
      - --cache-from=$_ARTIFACT_REPOSITORY_IMAGE_NAME:latest
      - .
    dir: "blog/backend"

  - id: push-packend
    name: "docker"
    args:
      - push
      - --all-tags
      - $_ARTIFACT_REPOSITORY_IMAGE_NAME
    dir: "blog/backend"
    waitFor: ["build-backend"]

  - id: "apply-migrations"
    name: "gcr.io/google-appengine/exec-wrapper"
    entrypoint: "bash"
    args: 
      - "-c"
      - "/buildstep/execute.sh -i $_ARTIFACT_REPOSITORY_IMAGE_NAME:$SHORT_SHA -e DATABASE_URL=$$DATABASE_URL -s $_CLOUDSQL_INSTANCE_FULL_NAME -- pnpm prisma migrate deploy"
    secretEnv: ["DATABASE_URL"]
    dir: "blog/backend"
    waitFor: ["push-backend"]

  - id: deploy-backend
    name: gcr.io/cloud-builders/gcloud
    args:
      - run
      - deploy
      - blog-backend
      - --quiet
      - --platform=managed
      - --project=$PROJECT_ID
      - --region=$_REGION
      - --image=$_ARTIFACT_REPOSITORY_IMAGE_NAME:$SHORT_SHA
      - --service-account=$_SERVICE_ACCOUNT
      - --add-cloudsql-instances=$_CLOUDSQL_INSTANCE_FULL_NAME
      - --revision-suffix=$SHORT_SHA
      - --tag=latest
      - --concurrency=40
      - --cpu=1
      - --memory=512Mi
      - --max-instances=3
      - --min-instances=0
      - --no-use-http2
      - allow-unauthenticated
      - --no-cpu-throttling
      - --ingress=all
      - --set-env-vars GCP_PROJECT_ID=$PROJECT_ID
      - --update-secrets DATABASE_URL=BLOG_DATABASE_URL:latest
      dir: "blog/backend"
      waitFor: ["apply-migrations"]

timeout: 2000s
substitutions: 
  _REGION: by-terraform
  _CLOUDSQL_INSTANCE_FULL_NAME: by-terraform
  _ARTIFACT_REPOSITORY_IMAGE_NAME: by-terraform
  _SERVICE_ACCOUNT: by-terraform
availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_NUMBER/secrets/BLOG_DATABASE_URL/versions/latest
      env: DATABASE_URL

images:
  - $_ARTIFACT_REPOSITORY_IMAGE_NAME:$SHORT_SHA